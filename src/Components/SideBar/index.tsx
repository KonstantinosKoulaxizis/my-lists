import CreateNewButton from '../CreateNewButton'

import './SideBar.scss'

const SideBar = () => {
  return (
    <div id="side-bar-container">
      <CreateNewButton />
      SideBar
    </div>
  )
}

export default SideBar
